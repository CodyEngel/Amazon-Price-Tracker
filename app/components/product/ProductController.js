(function() {
  
	var app = angular.module("unmarket");

	var ProductController = function($scope, $routeParams) {

	};
  
	app.controller("ProductController", ["$scope", "$routeParams", ProductController]); // array allows for minification
  
}());